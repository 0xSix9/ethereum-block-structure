# ethereum-block-structure
# Ethereum Block Structure

This repository provides a clear breakdown of the internal structure of an Ethereum block — the fundamental unit that makes up the Ethereum blockchain.

## Overview

Every Ethereum block consists of two main parts: the **Header** and the **Body**.
